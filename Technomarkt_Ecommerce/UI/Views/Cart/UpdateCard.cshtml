@model AppUserCard
@{
    ViewBag.Title = "UpdateCard";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section id="form">
    <div class="row">
        <div class="col-lg-4 mx-auto mt-5">
            <header>
                Update Card
            </header>
            <form method="post" enctype="multipart/form-data">
                <div class="form-group">
                    <label>Expiration Date</label>
                    @Html.TextBoxFor(x => x.ExpirationDate, new { @class = "form-control mt-3" })
                    <small>(MM/YY)</small>
                </div>
                <div class="form-group">
                    <label>CVC</label>
                    @Html.TextBoxFor(x => x.CVC, new { @class = "form-control mt-3" })
                </div>
                <button type="submit" class="btn btn-primary d-grid gap-2 col-3 mx-auto mt-3">Update</button>
            </form>
        </div>
    </div>
</section>

